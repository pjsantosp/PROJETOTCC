@model SISPTD.Models.Pessoa
<a title="Detalhes" class="glyphicon glyphicon-align-justify" href="@Url.Action("Details", "Pessoa", new { id = Model.pessoaId })"></a>

@if (User.IsInRole("Gerente"))
{
    <a title="Editar" class="glyphicon glyphicon-edit " href="@Url.Action("Edit", "Pessoa", new { id = Model.pessoaId })"></a>
    <a title="Delete"  class="glyphicon glyphicon-remove" href="@Url.Action("Delete", "Pessoa" ,new { id = Model.pessoaId })"></a>
    <a title="Acompanhante"  class="glyphicon glyphicon-paperclip" href="@Url.Action("CreateAcompanhante","Pessoa")"></a>
}
